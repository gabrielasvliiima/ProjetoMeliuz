<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.projetomeliuz">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".checkout.CheckoutActivity">
        </activity>
        <activity android:name=".productdetail.ProductDetailActivity">
        </activity>
        <activity android:name=".promotiondetails.view.PromotionDetailsActivity">
        </activity>
        <activity android:name=".advertisement.AdvertisementActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".promotions.view.PromotionsActivity"/>
    </application>

</manifest>